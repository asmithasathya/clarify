method:
  name: resample_clarify
  description: >
    Intent-stabilization method. Sample ambiguity and intent hypotheses,
    run a full repair pass when the intent state is unstable, and ask a
    generic clarification question only if confidence remains too low
    after the repair budget.
