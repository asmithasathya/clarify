method:
  name: resample_clarify
  description: >
    Intent-stabilization method. Sample ambiguity and intent hypotheses,
    selectively resample weak stages, and ask a clarification only if
    intent confidence remains too low after the repair budget.
